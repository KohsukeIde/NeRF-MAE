# Shortcut Probe Artifacts

Lightweight result artifacts referenced from `SHORTCUT_PROBE_EXPERIMENT_LOG.md`.

Included:
- `74` eval JSON files
- `1` CSV files

Excluded:
- checkpoints (`*.pt`)
- launcher logs and training logs
- proposal dumps and large intermediate outputs

See `manifest.json` for source-to-artifact mapping.
